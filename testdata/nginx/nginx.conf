worker_processes auto;

error_log /var/log/nginx/error.log info;

events {
    worker_connections  1024;
}

stream {
    proxy_protocol on;

    upstream backend {
        server host.docker.internal:33069;
    }

    server {
        listen 3308;
        proxy_pass backend;
    }
}